# 🔧 תיקון כפתור "ערוך דף"

## הבעיה
כשלוחצים על "ערוך דף" מקבלים דף לבן.

## מה תוקן

### 1. שיפור חיפוש הדף ב-`/manage/[pageId]/+page.server.js`
הוספנו 3 אסטרטגיות חיפוש:
- ✅ חיפוש לפי `documentId` (Strapi v5)
- ✅ חיפוש לפי `slug`
- ✅ חיפוש לפי `id` מספרי

```javascript
// Strategy 1: Try by documentId
pageResponse = await fetch(`${STRAPI_URL}/api/pages/${pageId}?populate=deep,3`...

// Strategy 2: Try by slug
pageResponse = await fetch(`${STRAPI_URL}/api/pages?filters[slug][$eq]=${pageId}...

// Strategy 3: Try by numeric ID
pageResponse = await fetch(`${STRAPI_URL}/api/pages?filters[id][$eq]=${pageId}...
```

### 2. הוספת לוגים לדיבאג
- ✅ לוג בשרת: איזה אסטרטגיה עבדה
- ✅ לוג בלקוח: מה ה-ID שנשלח
- ✅ לוג בדף הניהול: מה הנתונים שהתקבלו

### 3. תיקון fallback ל-`documentId`
```javascript
documentId: page.documentId || page.id
```

## איך לבדוק

1. פתח דף: `http://localhost:5173/view/[slug]`
2. לחץ על "ערוך דף" בסרגל העליון
3. בדוק בקונסול:
   - האם יש לוג "🔍 Looking for page"
   - האם יש לוג "✅ Found by..."
   - האם יש לוג "📄 Page loaded"

## אם עדיין לא עובד

### בדוק בקונסול של הדפדפן:
```
🆔 Page ID: 123
📄 Document ID: abc-123-def
🔗 Edit link will be: /manage/abc-123-def
```

### בדוק בקונסול של השרת:
```
🔍 Looking for page: abc-123-def
✅ Found by documentId
📄 Page loaded: שם הדף
```

### אם יש שגיאה 404:
1. בדוק ש-Strapi רץ על פורט 1337
2. בדוק שיש API token ב-`.env`
3. נסה לגשת ישירות: `http://localhost:1337/api/pages`

### אם יש דף לבן:
1. פתח את Developer Tools (F12)
2. לך ל-Console
3. חפש שגיאות אדומות
4. העתק את השגיאה ושלח לי

## קבצים שעודכנו

```
new-app/src/routes/manage/[pageId]/+page.server.js
├── ✅ 3 אסטרטגיות חיפוש
├── ✅ לוגים מפורטים
└── ✅ טיפול טוב יותר בשגיאות

new-app/src/routes/manage/[pageId]/+page.svelte
└── ✅ לוגים לדיבאג

new-app/src/routes/view/[slug]/+page.svelte
└── ✅ לוגים לדיבאג

new-app/src/routes/view/[slug]/+page.server.js
└── ✅ fallback ל-documentId
```

## מה הלאה

אם זה עדיין לא עובד, נצטרך:
1. לבדוק מה בדיוק מחזיר Strapi
2. לבדוק אם יש בעיה עם הרשאות
3. לבדוק אם יש בעיה עם CORS

---

**סטטוס:** 🔄 ממתין לבדיקה  
**הוראות:** רענן את הדפדפן (Ctrl+Shift+R) ונסה שוב
