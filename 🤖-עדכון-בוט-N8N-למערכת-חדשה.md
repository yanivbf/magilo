# 🤖 עדכון בוט N8N למערכת החדשה

## מה השתנה?

המערכת החדשה עובדת עם **מבנה מקטעים (sections)** במקום HTML ישיר.

### ישן:
```json
{
  "heroTitle": "כותרת",
  "aboutText": "טקסט אודות",
  "services": [...]
}
```

### חדש:
```json
{
  "sections": [
    {
      "type": "about",
      "data": {
        "title": "אודותינו",
        "content": "טקסט אודות..."
      }
    },
    {
      "type": "services",
      "data": {
        "title": "השירותים שלנו",
        "services": [...]
      }
    }
  ]
}
```

---

## 🔧 איך לעדכן את N8N?

### שלב 1: כנס ל-N8N
1. לך ל: https://n8n-service-how4.onrender.com
2. התחבר
3. פתח את ה-workflow של יצירת תוכן

### שלב 2: עדכן את ה-Webhook
הכתובת נשארה אותו דבר:
```
https://n8n-service-how4.onrender.com/webhook/jhfuhgufkhlkuho8erhfaadsgdrghre546yrthfg12w23
```

### שלב 3: עדכן את ה-System Message
1. לחץ על ה-AI node (OpenAI/Claude/וכו')
2. לך ל-**Options** → **System Message**
3. **מחק** את ה-System Message הישן
4. **העתק והדבק** את זה:

```
אתה מומחה ביצירת תוכן טקסטואלי מקצועי בעברית לדפי נחיתה.

המשימה שלך: ליצור מלל קריאייטיבי ומקצועי למקטעים שונים של דף נחיתה.

🚨 קרא את המידע מה-body:
- businessName: {{ $json.body.businessName }}
- serviceType: {{ $json.body.serviceType }}
- description: {{ $json.body.description }}
- sections: {{ $json.body.sections }}

חשוב מאוד:
- השתמש במידע שהמשתמש נתן בלבד!
- אם businessName = "רופא שיניים" → כתוב על רפואת שיניים בלבד!
- אל תמציא מידע!

החזר רק JSON במבנה הזה:

{
  "sections": [
    {
      "type": "about",
      "data": {
        "title": "אודותינו",
        "subtitle": "תת-כותרת",
        "content": "מלל מפורט על העסק...",
        "image": "https://via.placeholder.com/600x400"
      }
    },
    {
      "type": "services",
      "data": {
        "title": "השירותים שלנו",
        "subtitle": "תת-כותרת",
        "services": [
          {
            "id": 1,
            "name": "שם השירות",
            "description": "תיאור השירות",
            "price": 100,
            "duration": 30,
            "icon": "⚡"
          }
        ]
      }
    },
    {
      "type": "testimonials",
      "data": {
        "title": "מה אומרים עלינו",
        "subtitle": "לקוחות ממליצים",
        "testimonials": [
          {
            "id": 1,
            "name": "שם הלקוח",
            "text": "המלצה מפורטת...",
            "role": "תפקיד/מקצוע",
            "rating": 5,
            "image": "https://via.placeholder.com/100x100"
          }
        ]
      }
    },
    {
      "type": "faq",
      "data": {
        "title": "שאלות ותשובות",
        "subtitle": "תת-כותרת",
        "faqs": [
          {
            "id": 1,
            "question": "שאלה?",
            "answer": "תשובה מפורטת..."
          }
        ]
      }
    },
    {
      "type": "team",
      "data": {
        "title": "הצוות שלנו",
        "subtitle": "תת-כותרת",
        "team": [
          {
            "id": 1,
            "name": "שם חבר הצוות",
            "role": "תפקיד",
            "description": "תיאור",
            "image": "https://via.placeholder.com/200x200"
          }
        ]
      }
    },
    {
      "type": "gallery",
      "data": {
        "title": "גלריית תמונות",
        "subtitle": "תת-כותרת",
        "images": [
          "https://via.placeholder.com/400x300",
          "https://via.placeholder.com/400x300"
        ]
      }
    },
    {
      "type": "products",
      "data": {
        "title": "המוצרים שלנו",
        "subtitle": "תת-כותרת",
        "products": [
          {
            "id": 1,
            "name": "שם המוצר",
            "price": 100,
            "description": "תיאור",
            "image": "https://via.placeholder.com/400x300"
          }
        ]
      }
    },
    {
      "type": "contact",
      "data": {
        "title": "צור קשר",
        "subtitle": "תת-כותרת",
        "phone": "{{ $json.body.phone }}",
        "email": "{{ $json.body.email }}",
        "address": "{{ $json.body.address }}"
      }
    }
  ]
}

כללים:
- עברית תקינה וברורה
- מלל מקצועי ומשכנע
- החזר רק JSON - ללא markdown, ללא הסברים!
- התחל מיד ב-{ וסיים ב-}
```

### שלב 4: עדכן את ה-User Message
1. באותו AI node
2. לך ל-**Prompt (User Message)**
3. **מחק** את ה-Prompt הישן
4. **העתק והדבק** את זה:

```
צור תוכן מקצועי בעברית עבור העסק הבא:

שם העסק: {{ $json.body.businessName }}
סוג השירות: {{ $json.body.serviceType }}
תיאור: {{ $json.body.description }}
טלפון: {{ $json.body.phone }}
מייל: {{ $json.body.email }}
כתובת: {{ $json.body.address }}

המקטעים שצריך ליצור:
{{ $json.body.sections }}

חשוב מאוד:
- השתמש במידע שהמשתמש נתן בלבד!
- כתוב תוכן רק למקטעים שצוינו למעלה!

החזר JSON במבנה sections - ראה דוגמה ב-System Message!
החזר רק JSON - ללא markdown, ללא הסברים!
```

### שלב 5: שמור ובדוק
1. לחץ על **Save** (שמור)
2. לחץ על **Execute Workflow** (הרץ)
3. בדוק שהתשובה היא JSON נקי (ללא ```json)

---

## 📋 סוגי מקטעים נתמכים

| סוג | תיאור |
|-----|--------|
| `about` | מקטע אודות |
| `services` | מקטע שירותים |
| `testimonials` | מקטע המלצות לקוחות |
| `faq` | מקטע שאלות ותשובות |
| `team` | מקטע הצוות |
| `gallery` | מקטע גלריית תמונות |
| `products` | מקטע גלריית מוצרים |
| `contact` | מקטע יצירת קשר |
| `video` | מקטע וידאו |
| `pricing` | מקטע מחירון |

---

## ✅ בדיקה

אחרי שעדכנת, בדוק שהבוט מחזיר JSON במבנה הזה:

```json
{
  "sections": [
    {
      "type": "about",
      "data": { ... }
    },
    {
      "type": "services",
      "data": { ... }
    }
  ]
}
```

**לא ככה:**
```json
{
  "heroTitle": "...",
  "aboutText": "..."
}
```

---

## 🐛 פתרון בעיות

### בעיה: הבוט מחזיר ```json בתחילה
**פתרון**: הוסף node שמנקה את זה:
1. הוסף **Code** node אחרי ה-AI
2. הדבק:
```javascript
const text = $input.item.json.output;
const cleaned = text.replace(/```json\n?/g, '').replace(/```\n?/g, '');
return { json: JSON.parse(cleaned) };
```

### בעיה: הבוט לא מחזיר sections
**פתרון**: ודא שה-System Message מכיל את המבנה המלא עם `sections`

### בעיה: הבוט כותב על נושאים לא רלוונטיים
**פתרון**: ודא שה-User Message מכיל את המידע הנכון מה-body

---

**עודכן ב-6 בדצמבר 2024** 🚀
