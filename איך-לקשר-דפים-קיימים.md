# 🔗 איך לקשר דפים קיימים למשתמש

## הבעיה
יש 25 דפים ב-Strapi אבל אף אחד מהם לא מקושר למשתמש, לכן הם לא מופיעים בדשבורד.

## הפתרון

### שלב 1: מצא את ה-userId שלך

**אופציה א': מה-cookie**
1. פתח את הדפדפן
2. לחץ F12 (Developer Tools)
3. לך ל-Application → Cookies → `http://localhost:5173`
4. חפש את ה-cookie בשם `userId`
5. העתק את הערך

**אופציה ב': מ-localStorage**
1. פתח את הדפדפן
2. לחץ F12 (Developer Tools)
3. לך ל-Console
4. הקלד: `document.cookie.split('; ').find(row => row.startsWith('userId='))?.split('=')[1]`
5. העתק את הערך

**אופציה ג': צור משתמש חדש**
1. לך ל-`http://localhost:5173/set-user`
2. לחץ על "🎲 אקראי" ליצירת משתמש חדש
3. לחץ "✅ צור משתמש והתחבר"
4. העתק את ה-userId שנוצר

### שלב 2: הרץ את הסקריפט

```bash
node link-pages-to-user.js <YOUR_USER_ID>
```

**דוגמה:**
```bash
node link-pages-to-user.js f98bffa9-8af6-460f-8ac1-be8cb57266cb
```

### מה הסקריפט עושה?

1. ✅ מחפש את המשתמש ב-Strapi לפי userId
2. ✅ אם לא נמצא - יוצר משתמש חדש
3. ✅ מוצא את כל הדפים ב-Strapi (25 דפים)
4. ✅ מקשר כל דף למשתמש (מוסיף `user` relation ו-`userId` field)
5. ✅ מדלג על דפים שכבר מקושרים

### תוצאה צפויה

```
🔍 Looking for user with userId: f98bffa9-8af6-460f-8ac1-be8cb57266cb
✅ Found existing user with Strapi ID: 1

📄 Fetching all pages...
📊 Found 25 pages

✅ Linked page 1 (חתונה) to user 1
✅ Linked page 2 (DJ Adva) to user 1
✅ Linked page 3 (יניב צעצוע) to user 1
...
✅ Linked page 25 (DJ Adva) to user 1

🎉 Done! Linked 25 pages, skipped 0 pages
```

### שלב 3: רענן את הדשבורד

1. לך ל-`http://localhost:5173/dashboard`
2. רענן את הדף (F5)
3. כל 25 הדפים אמורים להופיע! 🎉

---

## קובץ הסקריפט
`link-pages-to-user.js`

---

**תאריך**: 5 בדצמבר 2025
**סטטוס**: ✅ מוכן לשימוש
