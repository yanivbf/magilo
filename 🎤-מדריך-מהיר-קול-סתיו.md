# 🎤 מדריך מהיר - קול סתיו

## 🚀 בדיקה מהירה (30 שניות)

### דרך 1: דף בדיקה
```
file:///[PATH]/test-stav-voice-marketplace.html
```
לחץ "נגן קול סתיו המקורי" ✅

### דרך 2: מרקטפלייס
```
http://localhost:5174/marketplace
```
1. לחץ על הבועה הסגולה 🤖
2. שלח הודעה
3. לחץ על 🔊

## 🎯 איך הקול צריך להישמע?

✅ **רובוטי** - לא טבעי  
✅ **מהיר** - 1.35x מהירות  
✅ **נשי** - אבל ממוכן  
✅ **עם אפקט רמקול**

## 🔧 איפה הקוד?

### ChatBotBubble.svelte
```javascript
async function speakMessage(text) {
    const API_KEY = 'AIzaSyBexR71l9bdJ28OKx8P7QhyQDQpqTPa56o';
    const response = await fetch(
        `https://texttospeech.googleapis.com/v1/text:synthesize?key=${API_KEY}`,
        { /* ... */ }
    );
    const data = await response.json();
    const audio = new Audio('data:audio/mp3;base64,' + data.audioContent);
    await audio.play();
}
```

### StavBot.svelte
אותו קוד בדיוק.

## 📍 מיקומים

- `new-app/src/lib/components/ChatBotBubble.svelte` - צ'אט בוט בדפים
- `new-app/src/lib/components/StavBot.svelte` - סתיו במרקטפלייס

## ⚙️ הגדרות

```javascript
{
    voice: 'he-IL-Wavenet-A',
    pitch: -5.0,      // רובוטי
    speed: 1.35,      // מהיר
    effects: ['small-bluetooth-speaker-class-device']
}
```

## 🔄 אם לא עובד?

1. בדוק שהשרת רץ: `netstat -ano | findstr "5174"`
2. בדוק Console בדפדפן (F12)
3. בדוק שיש אינטרנט (Google TTS צריך חיבור)
4. אם Google TTS נכשל - יעבור אוטומטית ל-TTS של הדפדפן

## ✅ סטטוס

- ✅ עובד במרקטפלייס
- ✅ עובד בצ'אט בוט
- ✅ Fallback מוכן
- ✅ ניקוי טקסט אוטומטי

---

**עדכון אחרון**: 9 בדצמבר 2025
