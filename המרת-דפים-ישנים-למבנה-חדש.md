# 🔄 המרת דפים ישנים למבנה חדש

## 🎯 המטרה
להמיר דפים ישנים שנשמרו עם `htmlContent` גולמי למבנה מובנה עם:
- `gallery` - מערך תמונות
- `testimonials` - מערך המלצות
- `faq` - מערך שאלות ותשובות
- `aboutText` - טקסט אודות

## 🔍 הבעיה
דפים ישנים נשמרו כך:
```json
{
  "htmlContent": "<div>...</div>",
  "metadata": {
    "gallery": ["url1", "url2"]
  }
}
```

אבל PageRenderer מצפה ל:
```json
{
  "gallery": ["url1", "url2"],
  "testimonials": [{name, role, text, rating}],
  "faq": [{question, answer}],
  "aboutText": "..."
}
```

## ✅ הפתרון
עדכנו את `+page.server.js` לחלץ את הנתונים מ-`metadata`:

```javascript
// Extract metadata for easier access
const metadata = page.attributes.metadata || {};

return {
  page: {
    // ... שאר השדות
    metadata: metadata,
    // Extract structured data from metadata for PageRenderer
    gallery: metadata.gallery || [],
    testimonials: page.attributes.testimonials || [],
    faq: page.attributes.faq || [],
    aboutText: page.attributes.aboutText || '',
  }
}
```

## 🎨 איך זה עובד עכשיו

### דפים חדשים
כשיוצרים דף חדש עם מקטעים אופציונליים:
1. הנתונים נשמרים גם ב-`metadata.gallery` וגם ב-`gallery`
2. PageRenderer מקבל את הנתונים המובנים
3. המקטעים מוצגים בעיצוב מלא

### דפים ישנים
1. הנתונים נמצאים ב-`metadata.gallery`
2. `+page.server.js` מחלץ אותם ל-`gallery`
3. PageRenderer מקבל את הנתונים
4. המקטעים מוצגים בעיצוב מלא

## 📝 מה צריך לעשות עכשיו

### אם יש דפים ישנים שלא עובדים:
1. פתח את דף הניהול של הדף
2. ערוך את הדף (אפילו בלי לשנות כלום)
3. שמור שוב
4. הדף יישמר עם המבנה החדש

### אם רוצה להמיר אוטומטית:
צריך ליצור סקריפט שעובר על כל הדפים ב-Strapi ומעדכן אותם:

```javascript
// scripts/migrate-old-pages.js
async function migrateOldPages() {
  const pages = await getAllPages();
  
  for (const page of pages) {
    if (page.metadata && !page.gallery) {
      // Extract data from metadata
      const updates = {
        gallery: page.metadata.gallery || [],
        // Add other fields...
      };
      
      await updatePage(page.id, updates);
    }
  }
}
```

## ✅ סטטוס
- ✅ PageRenderer מציג מקטעים מעוצבים
- ✅ דפים חדשים נשמרים עם מבנה מובנה
- ✅ `+page.server.js` מחלץ נתונים מ-metadata
- ⚠️ דפים ישנים צריכים המרה ידנית או אוטומטית
