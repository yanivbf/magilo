# 🤖 הגדרות קול רובוטי

## ⚙️ **הגדרות נוכחיות:**

```javascript
utterance.rate = 1.1;   // מהירות דיבור (1.0 = רגיל, 1.2 = מהיר)
utterance.pitch = 0.6;  // גובה הקול (0.6 = נמוך/רובוטי, 1.0 = רגיל)
utterance.volume = 1.0; // עוצמת קול (0.0-1.0)
```

---

## 🎚️ **אפשרויות שינוי:**

### **קול רובוטי מאוד** 🤖🤖🤖
```javascript
utterance.rate = 1.2;   // מהיר
utterance.pitch = 0.5;  // נמוך מאוד
```

### **קול רובוטי בינוני** 🤖🤖
```javascript
utterance.rate = 1.1;   // קצת מהיר
utterance.pitch = 0.6;  // נמוך
```
**← זה מה שמוגדר עכשיו**

### **קול טבעי יותר** 👤
```javascript
utterance.rate = 1.0;   // רגיל
utterance.pitch = 0.8;  // כמעט רגיל
```

### **קול נשי רגיל** 👩
```javascript
utterance.rate = 1.0;   // רגיל
utterance.pitch = 1.0;  // רגיל
```

### **קול גבוה/מצחיק** 🎪
```javascript
utterance.rate = 1.3;   // מהיר מאוד
utterance.pitch = 1.5;  // גבוה מאוד
```

---

## 🔧 **איך לשנות?**

### **שלב 1: פתח את הקובץ**
```
c:\Users\יניב\Desktop\AutoPage\public\marketplace.html
```

### **שלב 2: חפש (Ctrl+F):**
```
🤖 ROBOTIC VOICE
```

### **שלב 3: שנה את המספרים:**
```javascript
utterance.rate = 1.1;   // ← שנה את זה
utterance.pitch = 0.6;  // ← שנה את זה
```

### **שלב 4: שמור (Ctrl+S)**

### **שלב 5: רענן דפדפן**
```
Ctrl + Shift + R
```

---

## 🎯 **טיפים:**

### **pitch (גובה קול):**
- `0.5` = רובוט עמוק מאוד 🤖
- `0.6` = רובוט בינוני 🤖
- `0.8` = כמעט טבעי 👤
- `1.0` = טבעי לגמרי 👤
- `1.5` = גבוה/מצחיק 🎪

### **rate (מהירות):**
- `0.8` = איטי 🐢
- `1.0` = רגיל 🚶
- `1.1` = קצת מהיר 🏃
- `1.3` = מהיר מאוד 🚀

---

## 🎙️ **קולות זמינים:**

המערכת מנסה למצוא קול עברי אוטומטית. אם אין, היא משתמשת באנגלית.

לבדוק אילו קולות יש במחשב שלך:
1. פתח F12 (קונסול)
2. הקלד:
```javascript
speechSynthesis.getVoices().forEach(v => console.log(v.name, v.lang))
```
3. תראה רשימת כל הקולות

---

## 💡 **המלצה:**

אם אתה רוצה קול רובוטי **אבל מובן**:
```javascript
utterance.rate = 1.1;
utterance.pitch = 0.7;  // ← קצת יותר גבוה מ-0.6
```

זה יהיה רובוטי אבל לא מדי נמוך.

---

## 🔄 **חזרה לרגיל:**

```javascript
utterance.rate = 1.0;
utterance.pitch = 1.0;
```

