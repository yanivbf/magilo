# תוכנית בניית מערכת מובנית חדשה

## הבעיה הנוכחית
המערכת שומרת HTML מלא ב-Strapi במקום מבנה מובנה (structured data).
זה גורם לבעיות:
- לא ניתן לערוך מקטעים בנפרד
- לא ניתן להוסיף/להסיר מקטעים בקלות
- קשה לנהל תוכן דינמי
- גלריית מוצרים לא נשמרת כראוי

## הפתרון - מערכת מובנית חדשה

### 1. מבנה הנתונים ב-Strapi
```
Page {
  title: string
  slug: string
  pageType: string
  userId: string
  
  // Basic Info
  phone: string
  email: string
  city: string
  address: string
  description: string
  
  // Metadata (JSON)
  metadata: {
    videoUrl: string
    socialLinks: {...}
    headerImage: string
  }
  
  // Relations
  sections: [Section]  // One-to-Many
  products: [Product]  // One-to-Many (for stores)
  services: [Service]  // One-to-Many (for service providers)
}

Section {
  type: enum ['hero', 'about', 'gallery', 'faq', 'testimonials', 'products', 'contact']
  enabled: boolean
  order: number
  data: JSON  // Flexible data per section type
  page: Page  // Many-to-One
}

Product {
  name: string
  description: string
  price: number
  image: string
  enabled: boolean
  order: number
  page: Page  // Many-to-One
}
```

### 2. תהליך יצירת דף חדש

#### שלב 1: SmartPageCreator שולח נתונים מובנים
```javascript
{
  userId: "user_123",
  pageType: "store",
  title: "החנות שלי",
  mainName: "חנות האופנה",
  phone: "050-1234567",
  email: "store@example.com",
  description: "חנות אופנה מעולה",
  
  // Products array
  products: [
    { name: "מוצר 1", price: 99, image: "...", description: "..." },
    { name: "מוצר 2", price: 149, image: "...", description: "..." }
  ],
  
  // Optional sections flags
  optionalSections: ['gallery', 'faq', 'testimonials', 'about']
}
```

#### שלב 2: API יוצר דף + מקטעים
```javascript
// Create page
const page = await createPage({
  title, slug, pageType, userId,
  phone, email, city, address, description,
  metadata: { videoUrl, socialLinks, headerImage }
});

// Create sections based on optionalSections
if (optionalSections.includes('gallery')) {
  await createSection({
    type: 'gallery',
    enabled: true,
    order: 1,
    data: {
      title: 'גלריית תמונות',
      images: [...]
    },
    page: page.id
  });
}

// Create products for store
if (pageType === 'store') {
  for (const product of products) {
    await createProduct({
      ...product,
      page: page.id
    });
  }
}
```

#### שלב 3: PageRenderer מרנדר מבנה מובנה
```svelte
<!-- Hero Section -->
<HeroSection data={page} />

<!-- Dynamic Sections -->
{#each page.sections as section}
  {#if section.enabled}
    {#if section.type === 'gallery'}
      <GallerySection data={section.data} />
    {:else if section.type === 'faq'}
      <FAQSection data={section.data} />
    {:else if section.type === 'products'}
      <ProductsGallerySection products={page.products} />
    {/if}
  {/if}
{/each}

<!-- Contact Section -->
<ContactSection data={page} />
```

### 3. קבצים שצריך לשנות

1. **SmartPageCreator.svelte** - שולח נתונים מובנים
2. **create-page-with-template/+server.js** - יוצר דף + מקטעים מובנים
3. **PageRenderer.svelte** - מרנדר מבנה מובנה (לא HTML)
4. **pages/[slug]/+page.server.js** - טוען נתונים מובנים
5. **strapi.js** - פונקציות ליצירת sections ו-products

### 4. יתרונות המערכת החדשה

✅ ניתן לערוך כל מקטע בנפרד
✅ ניתן להוסיף/להסיר מקטעים בקלות
✅ ניתן לשנות סדר מקטעים
✅ גלריית מוצרים עובדת תמיד
✅ קל לנהל תוכן דינמי
✅ אין HTML מלוכלך ב-database

## הצעדים הבאים

1. ✅ הבנו את הבעיה
2. 🔄 נבנה את המערכת החדשה צעד אחר צעד
3. ⏳ נבדוק שהכל עובד
4. ⏳ נמחק את הקוד הישן
