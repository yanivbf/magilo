# 🔧 תיקון שגיאת 500 ביצירת דף

## השגיאה
```
Failed to load resource: the server responded with a status of 500 (Internal Server Error)
api/create-structured-page:1
```

## מה קרה?
יצרת דף חדש והשרת החזיר שגיאת 500.

## סיבות אפשריות

### 1. בעיה ב-Strapi
- Strapi לא רץ
- בעיה בחיבור ל-Strapi
- שדה חסר בסכמה

### 2. בעיה ב-N8N
- N8N לא מגיב
- timeout בקריאה
- תשובה לא תקינה

### 3. בעיה בנתונים
- `designStyle` לא נשמר נכון
- שדה חובה חסר
- פורמט לא תקין

## איך לבדוק?

### שלב 1: בדוק אם Strapi רץ
```cmd
curl http://localhost:1337/api/pages
```

אם מקבל שגיאה - Strapi לא רץ!

### שלב 2: בדוק את הלוגים
פתח את הטרמינל שבו רץ `npm run dev` ותראה את השגיאה המדויקת.

### שלב 3: נסה ליצור דף פשוט
```javascript
// test-create-page-error.js
node test-create-page-error.js
```

## תיקון זמני
אם הבעיה היא ב-N8N, אפשר לכבות את הקריאה אליו זמנית:

```javascript
// בקובץ create-structured-page/+server.js
// שורה ~145
let aiGeneratedContent = null;
if (false) { // שנה מ-pageData.mainName ל-false
    // ... קוד N8N
}
```

## מה לעשות עכשיו?
1. בדוק אם Strapi רץ
2. הסתכל על הלוגים בטרמינל
3. העתק את השגיאה המדויקת
4. תגיד לי מה השגיאה ואתקן

---

**צריך לראות את הלוגים בטרמינל! 📋**
